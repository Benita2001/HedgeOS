# HedgeOS — Autonomous Session Progress Log

Session: overnight autonomous continuation, user asleep, authorized to inspect/implement/refactor/test/document within the stated boundaries (no credentials, no live orders, no push, no deploy, no submission).

## Baseline (verified before starting)
- 31/31 tests passing, repo untracked, zero commits. Verified by actually running `vitest` and `tsc --noEmit`, not by trusting the prior report.
- Committed baseline (`7692345`).

## Checkpoint 1 — Decimal-safe arithmetic (Priority 1) — commit `ba99f86`
- `src/engine/decimal.ts`: exact BigInt fixed-point math (1e8 scale). `src/engine/sizing.ts` rewritten to use it throughout.
- 5 new tests including a direct regression test for the exact float-rounding bug from Milestone 1 (20/233 at step 0.01 would round UP to 0.09 without exact arithmetic).
- Runtime-verified against real NVDA market data — same results as before, no regression.

## Checkpoint 2 — Fixed bStock/TradFi identity validation bug — commit `67f9116`
- Old bug: any `<TICKER>BUSDT`-shaped symbol was accepted as a bStock purely by naming pattern.
- Fix: futures leg now requires Binance's own `underlyingType: EQUITY` + `underlyingSubType` including `"TradFi"`; spot leg requires `baseAsset` to exactly equal `<TICKER>B`; both cross-checked against each other.
- Added: 10s request timeout, one retry on HTTP 429 (honoring `Retry-After`), `source`/`observedAt` labels on every observation.
- **Runtime-verified, not just mocked**: real live call for `NVDA` still accepted; real live call for `BTC` now correctly rejected (`contractType=PERPETUAL, underlyingType=COIN, underlyingSubType=["PoW"]`) — proof the fix discriminates using Binance's actual classification, not a name guess.
- 5 new mocked tests (`tests/binanceClient.test.ts`).

## Checkpoint 3 — Partial fills and rejections (Priority 3) — commit `c048abe`
- `PaperExecutionAdapter` gained an injectable `simulateFill` hook (partial fill, rejection, slippage), defaulting to prior full-fill/zero-slippage behavior — existing demos unaffected unless configured.
- `runContribution` now distinguishes REQUESTED vs. FILLED quantity/notional per leg and persists both. New execution status `partial_failure` for "one leg filled, one rejected."
- Scheduler treats `partial_failure` as `failed_terminal` — **never auto-retried**, because retrying would double the leg that already executed. This is the concrete answer to "an uncertain execution result must never trigger blind duplicate order replay."
- `getPaperState()` now aggregates from actual filled amounts, never requested amounts.
- Reset the local disposable dev DB (schema evolved; `data/hedgeos.db` only ever held throwaway paper-mode demo data from this session, gitignored, safe to reset).
- **Re-verified restart-safety end-to-end after all changes so far**: seeded a strategy, ran the worker, got exactly 1 execution; restarted the worker; confirmed zero duplicate executions and zero new cycles (next_due_at correctly advanced to next day).
- 6 new tests (2 in `runContribution.test.ts`, 1 in `scheduler.test.ts` specifically proving the partial-failure cycle never re-enters the retry queue).

## Checkpoint 4 — HedgeOS-owned MCP server + minimal risk checks (Priority 4) — commit `7722663`
- `src/risk/checks.ts`: deterministic risk alerts — missing hedge exposure (deferred budget accumulating), stale schedule, execution reconciliation discrepancy, and a static (explicitly labeled non-live) leverage/maintenance-margin headroom note. Deliberately does **not** claim real-time liquidation-proximity monitoring, since that needs an ongoing mark-to-market price feed that doesn't exist yet — documented as a gap, not faked.
- `src/mcp/server.ts`: read tools (`list_strategies`, `get_strategy_status`, `list_recent_cycles`, `list_receipts`, `list_executions`, `preview_strategy`) and state-changing tools (`create_paper_strategy`, `pause_strategy`, `resume_strategy`, `trigger_due_cycle`), all Zod-schema-validated, with read/write MCP annotations. Operates on the same SQLite file the worker uses — doesn't need the worker running to answer reads.
- **Real end-to-end smoke test** (`scripts/mcp-smoke-test.ts`, not mocked): spawns the actual server as a subprocess, connects a real MCP `Client` over real stdio, and specifically proves the critical safety property — calling `trigger_due_cycle` twice back-to-back results in exactly one execution (`contributionsCount === 1`), because the second call hits the same idempotent claim path and finds nothing left to claim. All checks passed against live NVDA market data.
- `docs/MCP_SETUP.md` written with the exact `claude mcp add` command and tool inventory.

## Checkpoint 5 — Disabled live-execution primitives (Priority 6) — commit `d7df7f8`
- `src/binance/liveSigning.ts` / `liveRequests.ts`: real HMAC-SHA256 signing, signed-request builders for the verified official endpoints (new order for both spot and futures, leverage, margin type, position risk), and `reconcileOrder()` — which never trusts an order-placement response alone: it independently sums actual user trades, computes a real volume-weighted average fill price, and flags a discrepancy rather than accepting a mismatched `executedQty`.
- 12 new tests against fixtures and fake credentials — zero network calls to production endpoints.
- **Confirmed genuinely unreachable**: grepped `src/worker`, `src/mcp`, `src/scheduler`, `src/binance/execution.ts` for any reference to these modules — none exists. `LiveExecutionAdapter` in `execution.ts` still throws unconditionally; the worker still hard-refuses `HEDGEOS_MODE=live`. This is defense in depth, not just "off by default."

## Checkpoint 6 — Minimal read-only dashboard (Priority 5) — commit pending
- `src/dashboard/server.ts`: plain Express, server-rendered HTML, no build step. Strategy list, per-strategy accumulated paper position, deterministic risk alerts, and cycle history with per-leg requested-vs-filled detail. Prominent "PAPER MODE" banner on every page, states the active adapter mode explicitly.
- Read-only: no route mutates anything. Reads the same SQLite DB the worker/MCP server use.
- **Verified with real `curl` requests against the real database** (not just "it compiles"): `GET /` lists the real strategy from the combined demo below; `GET /strategy/1` shows the real accumulated position (0.386 NVDABUSDT, 0.08 NVDAUSDT, $9.32 collateral), the real risk alert, and the real cycle history row with actual fill prices.

## Combined end-to-end demonstration (fresh DB, real data, run after all checkpoints above)
1. `scripts/seed-strategy.ts NVDA 100 2 daily` → strategy #1 created, due immediately.
2. Persistent worker started → detected the due cycle, ran real discovery (`NVDABUSDT`/`NVDAUSDT` live prices), executed a simulated 90/10 fill, persisted the receipt — all within ~3 seconds, one execution.
3. Worker stopped (SIGINT) and **restarted** → zero interrupted cycles found, zero new cycles created (next_due_at correctly advanced to tomorrow) — no duplicate.
4. HedgeOS's own MCP server queried via a real MCP client against the **same** database: `list_strategies` shows strategy #1; `get_strategy_status` shows `contributionsCount: 1`, correct accumulated stock/hedge quantities, and one info-level risk alert (static leverage headroom note).

This is the reproducible demo path from the standing instructions, run for real, not asserted from memory.

## Checkpoint 7 — VPS deployment and remote operation
- **Repository re-verified before trusting the prior report**: 56/56 tests, 8 commits, clean status — confirmed by actually running the commands, not by trusting the report.
- Read-only inspection of the target VPS (root@<vps-host, redacted from public history — see docs/SELF_HOSTED_INSTALL.md>, Ubuntu 24.04, 4 vCPU, 7.8GB RAM, 89GB disk) before any change: found Hermes runs as a **root user-level systemd unit** (`hermes-gateway.service`, invisible to system-level `systemctl`), `/opt/firecrawl` as a separate existing project, `ufw` **inactive** (no OS firewall at all), and only port 22 listening.
- **Real bug caught during deployment, not assumed away**: `/usr/local/bin/node`/`npx` turned out to be symlinks into Hermes' own private Node install under `/root/.hermes/` — unreadable to an unprivileged user. Rather than touch `/root` permissions or Hermes' install, gave HedgeOS its own fully independent Node v24.20.0 under `/opt/hedgeos/node`, verified the dedicated `hedgeos` system user can execute it, and pointed the systemd units at it explicitly (`Environment=PATH=/opt/hedgeos/node/bin:...`).
- Fixed the dashboard's `app.listen(port)` to bind explicitly to `127.0.0.1` by default (`src/dashboard/server.ts`) — it previously defaulted to all interfaces, which on a host with no firewall would have been immediately publicly reachable. Verified with `ss -tlnp` on the VPS after deploy: bound to `127.0.0.1:8766` only.
- Deployed: dedicated unprivileged system user `hedgeos` (no login, no password); `/opt/hedgeos/app` (rsynced from the local machine — no git push, no GitHub involved); `hedgeos-worker.service`, `hedgeos-dashboard.service`, `hedgeos-backup.timer`+`.service` (systemd, `Restart=always`, `NoNewPrivileges=true`, `ProtectSystem=strict`, `enabled` for boot).
- **Runtime-verified on the actual deployed service**, not assumed from local tests:
  1. `systemctl status` — both services `active (running)`.
  2. Created a real NVDA paper strategy via `scripts/seed-strategy.ts` run as the `hedgeos` user.
  3. Worker's 60s tick picked it up, ran real discovery against live Binance market data, executed: `strategy 1 (NVDA): created 1 due cycle -> claimed -> completed`.
  4. Inspected the durable receipt directly via `sqlite3`: real 90/10 split, `NVDABUSDT` BUY 0.385 @ $233.39, `NVDAUSDT` SELL 0.08 @ $233.36, $9.34 collateral — matches expected sizing exactly.
  5. `systemctl restart hedgeos-worker` — confirmed via `journalctl` ("no interrupted cycles found at startup") and a direct `SELECT COUNT(*) FROM executions` (still 1) that restart did **not** duplicate the execution.
  6. `curl`'d the dashboard on the VPS itself (loopback-only) — rendered the real accumulated position and cycle history.
  7. **Tested the HedgeOS MCP server exactly as Claude Code would use it**: spawned over SSH from the local Mac (`scripts/mcp-remote-test.ts`), using the same existing SSH key access, no new port or credential — `list_strategies` and `get_strategy_status` both returned the real deployed state.
  8. Confirmed both services `active` and `enabled` (start-on-boot) at the end of the session.
- **Explicitly not claimed**: a full machine reboot was not tested (would disrupt Hermes/firecrawl on a shared host without separate approval) — only `systemctl restart` and process-crash auto-restart (`Restart=always`) were verified. `systemctl is-enabled` confirms the units *would* start on boot, but that specific behavior is unverified.
- **Binance integration boundary confirmed**: the persistent service (worker, dashboard, MCP server) uses **public REST only** (`src/binance/client.ts`) — not the Binance Agent OS MCP server, which requires interactive browser OAuth and has no headless/service-account authentication route available. This was already true before this session; explicitly re-verified by reading the actual import graph, not assumed.
- No firewall rule changed, no new port opened beyond the pre-existing SSH, Hermes and firecrawl untouched, no credentials created, no live trading enabled, nothing pushed to GitHub, dashboard not made public.

## Checkpoint 8 — Binance Agent OS MCP operator integration (2026-09-08 build-review session)
- **First real Binance Agent OS MCP calls made and verified from an interactive session** (previous sessions could not connect it): `spot_tickerPrice(NVDABUSDT)` -> `228.11`, `spot_exchangeInfo(NVDABUSDT)` -> `baseAsset: "NVDAB"`/TRADING/minNotional 5, `futures_usds_symbolPriceTicker(NVDAUSDT)` -> `227.85`, `futures_usds_exchangeInformation` (1.1MB dump, filtered with `jq` to the `NVDAUSDT` entry) -> `contractType: "TRADIFI_PERPETUAL"`, `underlyingType: "EQUITY"`, `underlyingSubType: ["TradFi"]`, TRADING. `docs/INTEGRATION_SURFACES.md` §1 updated from "not connected" to verified, with the exact tool calls and results.
- `src/observations/externalObservation.ts`: `ExternalObservation` Zod schema + `validateExternalObservation()` — deterministic cross-check of an operator-supplied observation (symbol identity against HedgeOS's own live discovery, ≤120s freshness, ≤2% price deviation from HedgeOS's own price). Pure function, no network calls, 8 new unit tests including a contract-level test that the observation's `price` field is never the value sizing consumes.
- `src/mcp/server.ts`: new read-only tool `preview_with_agent_os_observations` — same discovery+sizing as `preview_strategy`, plus per-observation verdicts. Existing tools (`preview_strategy`, `create_paper_strategy`, `trigger_due_cycle`, all read tools) are unmodified.
- `scripts/agent-os-integration-demo.ts`: real end-to-end run (not mocked) — spawns the actual MCP server subprocess, feeds it the two real Agent OS observations captured above, calls `preview_with_agent_os_observations` (both observations accepted, deviation 0.02%/0.13% from HedgeOS's own concurrently-fetched live price), `create_paper_strategy`, `trigger_due_cycle` (one completed simulated paper fill), then verifies via `get_strategy_status`/`list_receipts`. Full transcript captured during this session; disposable test DB cleaned up after the run (gitignored, never committed).
- `docs/AGENT_OS_OPERATOR_WORKFLOW.md`: new operator-facing walkthrough distinguishing the three call types in this flow (Agent OS read = evidence only, public REST = authoritative sizing input, paper execution = simulated fill) and the reproduction command.
- **64/64 tests passing** (56 prior + 8 new), typecheck clean.
- **Explicitly unchanged**: the persistent VPS worker (`src/worker/index.ts`) still imports only `src/binance/client.ts` (public REST) — grepped again to confirm no reference to Agent OS or the new observation module. No credentials created, no live order placed, nothing pushed, nothing deployed, Hermes/firecrawl untouched, no public port opened.

## Checkpoint 9 — Real, gated, tested live-execution architecture (2026-09-08, second build-review session, ~9h to deadline)
User explicitly confirmed the product goal is real authorized execution, not paper-only; this checkpoint builds the complete execution path while keeping it fully inert (no credentials exist).

- **Doc re-verification** (official Binance docs, fetched live this session): New Order (`POST /fapi/v1/order`) full parameter table confirmed — `positionSide`, `reduceOnly`, and critically `newOrderRespType` (`ACK` default returns no fill data; `RESULT` returns it synchronously). Change-log re-confirmed the `/fapi/v1/stock/contract` TradFi-Perps agreement endpoint (2025-12-11) and its Portfolio-Margin counterpart (2026-04-08).
- `src/binance/liveHttp.ts` (new): real signed-HTTP transport (`RealLiveHttpClient`) plus two typed error classes — `BinanceApiError` (Binance received and rejected the request) vs. `AmbiguousOutcomeError` (timeout/network failure — outcome genuinely unknown). This distinction is the basis for the entire "never blindly retry" safety property.
- `src/binance/liveRequests.ts`: extended with `buildQuerySpotOrderRequest`, `buildSpotMyTradesRequest`, `buildSpotAccountRequest`, `buildFuturesAccountV3Request`, `buildFuturesOpenOrdersRequest`, `buildGetPositionSideDualRequest`; both new-order builders now set `newOrderRespType: "RESULT"`; futures builder gained `positionSide`/`reduceOnly`.
- `src/binance/liveExecution.ts` (new): the real orchestration layer —
  - `assertLiveTradingGate()`: fail-closed, four independent required conditions (mode, two explicit confirmations, real credentials), each checked and named individually.
  - `placeOrderWithAmbiguityRecovery()`: on a timeout/network error OR a duplicate-clientOrderId-shaped rejection, queries the order by its stable clientOrderId BEFORE ever considering a resubmission; a genuinely-not-found order (-2013) is surfaced as safe-to-place-once, never auto-retried; any other unresolved state throws loudly rather than guessing.
  - `placeAndReconcileFuturesOrder` / `placeAndReconcileSpotOrder`: place (with recovery) + independently reconcile via `userTrades`/`myTrades` (reusing the existing `reconcileOrder`, unchanged).
  - `configureHedgeAccount()`: sets leverage + ISOLATED margin before the hedge order; treats "-4046 no need to change" as a no-op, rethrows anything else (e.g. a margin-type change blocked by an existing position) rather than silently continuing.
  - `runLivePreflight()`: authenticated read-only account/permission/position-mode/open-orders checks, aggregated into a report rather than throwing on partial failure.
  - `LiveExecutionAdapter`: the real `ExecutionAdapter` implementation — refuses to place any order without an `OrderIdempotencyContext` (strategyId/cycleId/leg), routes hedge orders through leverage/margin config first, maps a still-non-terminal reconciliation outcome to `rejected`/unresolved rather than ever reporting it as filled.
- `src/binance/execution.ts`: `ExecutionAdapter.placeOrder` gained an optional `idempotencyContext` parameter (backward compatible — paper adapter ignores it); `getExecutionAdapter()` is now `async` and constructs `LiveExecutionAdapter` only after `assertLiveTradingGate()` passes. All three call sites updated (`worker/index.ts`, `mcp/server.ts`; `dashboard/server.ts` no longer calls it at all — reads the mode label straight from the env var instead, so the read-only dashboard can never itself trigger the live gate).
- `src/worker/index.ts`: removed the old unconditional "refuses to start under `HEDGEOS_MODE=live`" hard-code — replaced with the real, more precise gate in `assertLiveTradingGate`, which still refuses (with a specific, named reason) since none of its four conditions are set anywhere in this project's environment or deployment.
- `src/worker/runContribution.ts`: **hedge sizing correctness fix for live mode** — the hedge budget is now re-derived from the stock leg's ACTUAL FILLED NOTIONAL (not the pre-trade budgeted target) before sizing the hedge leg, preserving the 90/10 ratio applied to realized dollars rather than planned dollars. Paper mode deliberately unchanged (verified by a dedicated test) — this only activates when `adapter.mode === "live"`. Also threads a new optional `cycleId` parameter through to `adapter.placeOrder` so live orders get a stable, cycle-derived clientOrderId; `scheduler/cycles.ts#processCycle` now passes `cycle.id`.
- **27 new tests** (`tests/liveExecution.test.ts`): fail-closed gate (each of the 4 conditions individually), successful two-leg-shaped execution, reconciliation discrepancy, rejected orders, timeout-before-acceptance (recovered as never-placed), timeout-after-acceptance (recovered via query, confirmed exactly one `place()` call), duplicate-clientOrderId recovery, a genuinely-unresolvable ambiguous case (refuses to guess), insufficient-margin/permission/agreement-eligibility errors propagating unmodified (never misclassified as transient), leverage/margin-type configuration (success, idempotent no-op, and a blocked-by-existing-position failure), preflight aggregation (including partial-failure and position-mode variants), and the adapter's own refuse-without-idempotency-context / route-through-config / never-report-unresolved-as-filled behaviors. **3 new tests** in `tests/runContribution.test.ts` for the actual-fill-based hedge resizing, idempotency-context plumbing, and paper-mode non-regression.
- **Runtime-verified, not just unit-tested**: ran the actual `getExecutionAdapter()` code path as a real process three times with `HEDGEOS_MODE=live` and 0/1/2 of the remaining three gate conditions set — each run threw the correct, specifically-named refusal (verified live, not asserted from the test file). Re-ran `scripts/mcp-smoke-test.ts` end-to-end against real live market data after all this rewiring — unchanged pass, confirming the paper path was not disturbed.
- `scripts/find-min-live-test-budget.ts` (new): deterministic script computing the actual smallest exchange-valid contribution at 2×/3× leverage against real filters — used to replace a prior session's rejected $30/3× test proposal with the real calculated number ($35 minimum at the default 2×, per real live filters captured this session).
- `LIVE_TRADING_READINESS.md`: substantially rewritten — both legs now covered (previously hedge-leg-only framing), the live-code-implemented status stated with evidence, §4 budget corrected to the 2×-default calculated minimum with the 3×/$30 proposal explicitly withdrawn as noncompliant with frozen policy, §5 split into a code-enforced Layer A gate and a human/operational Layer B gate, §6/GO-NO-GO updated to reflect "architecture GO, account NO-GO."
- **94/94 tests passing** (64 prior + 27 + 3), typecheck clean.
- **Explicitly NOT done**: no credentials created, no agreement signed, no live order placed, no live-enabled code deployed to the VPS, nothing pushed, nothing exposed on a public port. Hermes/firecrawl untouched. The Agentic-sub-account-Futures-support question (§3 of LIVE_TRADING_READINESS.md) remains genuinely unverified — not assumed either way.

## Checkpoint 10 — Real read-only preflight run, code audit, generic-sizing audit (2026-09-08 ~17:00-17:50 UTC)
User rotated the API key that was pasted into chat, installed a fresh one on the VPS via a separate protected file (`/opt/hedgeos/secrets/live-preflight.env`, mode 600, owned `hedgeos`), and authorized syncing only the read-only preflight code (3 files, dry-run confirmed first) to run it.

- **Real preflight run** (`scripts/live-preflight.ts`, as the `hedgeos` user, against the real main-account credential): 10/10 checks passed. Permissions correctly scoped (Spot+Margin+Futures trade-enabled, withdrawals **disabled**, IP-restricted). One-way position mode. No existing NVDAUSDT position or open orders on either leg. **Both wallets currently at $0** (no USDT in Spot, no margin in Futures) — this is the actual current blocker, not permissions.
- **Critical defect found and fixed**: `runContribution.ts` had no try/catch around `adapter.placeOrder` — a THROWN error from the hedge leg (insufficient margin, permission failure, or a genuinely unresolved ambiguous-outcome error from `LiveExecutionAdapter`) propagated straight out of the function before the `INSERT INTO executions`/`receipts` calls ran. This meant a stock leg that had genuinely, exchange-confirmed FILLED could be lost with **zero record anywhere in HedgeOS** — invisible to every read tool, dashboard, and receipt query, while a real position sat on the exchange. Fixed with `placeOrderCapturingThrow()`: every `placeOrder` call site is now wrapped so a throw becomes a synthetic Fill with `status:"rejected"` and a `reason` explicitly prefixed `UNRESOLVED` (never conflated with a confirmed exchange rejection — the reason text tells a human to verify actual exchange state before assuming zero exposure). 2 new regression tests prove the stock leg's real fill now survives a hedge-leg throw, and that a stock-leg throw is captured the same way.
- **Second defect found and fixed**: reconciliation (`placeAndReconcileFuturesOrder`/`SpotOrder`) read `userTrades`/`myTrades` exactly once immediately after order placement — a MARKET order's own response can say FILLED microseconds before the trades-query endpoint has indexed the fill (eventual consistency), which would have downgraded a real, complete fill to `partially_filled`. Fixed with `fetchTradesWithReconciliationRetry()`: retries up to 3 times with a short delay ONLY while the order claims FILLED/PARTIALLY_FILLED but the independently-read trades don't yet agree; a genuine, stable discrepancy is still surfaced as such.
- **Third defect found and fixed**: `LiveExecutionAdapter` hardcoded `positionSide: "BOTH"` on every hedge order. Per official docs, Binance requires `LONG`/`SHORT` (never `BOTH`) when the account is in Hedge Mode, and rejects an order that gets this wrong. This account is one-way mode (confirmed via the real preflight), so it would have worked *by coincidence* — the code is now correct generically: `resolvePositionSide()` reads the account's real position mode once (cached per adapter instance) and derives `BOTH` or `SHORT` accordingly. New test proves `positionSide=SHORT` is sent when the account is in Hedge Mode.
- **Generic-sizing audit** (explicit requirement this session): `grep`'d `src/` for hardcoded `40`/`100`/`500`/`NVDA` — zero hardcoded contribution amounts or ticker-specific branches found; every hit was universal cent-rounding math or a pre-existing, already-labeled maintenance-margin estimate. `tests/contributionSizeSweep.test.ts` (10 new tests): runs the actual `sizeDcaHedgeContribution` engine across two below-minimum amounts, the calculated minimum-valid, $40, $100, $500, $50,000, and $1,000,000 — proves the 90/10 split, budget invariants, step-snapping, and leverage-proportional notional hold at every size, with small contributions deferring safely rather than failing silently or being forced through.
- **Recalculated minimum live-test budget** against a fresh real price (Binance Agent OS MCP, `spot_tickerPrice`/`futures_usds_symbolPriceTicker`, $226.16): $34 at 2× (was $35 a few hours earlier at $228.14 — expected drift, which is why sizing always re-fetches live price at execution time). Ran a real dry-run (`previewContribution("NVDA", 40, 2)`) showing exact planned symbols/quantities/notional/fees, clearly labeled as estimates pending exchange confirmation.
- **`LIVE_TRADING_READINESS.md` corrected**: removed the "$40 hard maximum-loss cap" framing per explicit user correction — reframed as a capital budget, with isolated margin explicitly stated as not an absolute loss guarantee (funding fees, liquidation fee, gap risk). Added §3a with exact Spot/Futures USDT funding amounts (minimum vs. recommended-with-buffer) and §8 with the generic-sizing audit evidence.
- **107/107 tests passing** (94 prior + 2 critical regression + 10 sizing-generality + 1 position-mode), typecheck clean, both locally and on the VPS (`/opt/hedgeos/node/bin/npx tsc --noEmit`).
- **Explicitly NOT done**: no order placed, no leverage/margin actually changed on the real account (only read endpoints called), no agreement signed, no funds transferred, no new credentials created, no permission changes, paper worker/dashboard confirmed still running unrestarted (`ActiveEnterTimestamp` unchanged) throughout. Hermes/firecrawl untouched. Only 3 files (`liveRequests.ts`, `liveHttp.ts`, `live-preflight.ts`) were synced to the VPS, dry-run confirmed first; `execution.ts`/`liveExecution.ts` deliberately NOT synced yet (would mismatch the VPS's still-old, un-updated `worker/index.ts`/`mcp/server.ts` callers).

## Checkpoint 11 — Leverage/margin verification, generic-sizing re-confirmation (2026-09-08 ~18:00 UTC)
User opened NVDAUSDT in the Binance app: market open, order form available, **no agreement/risk-quiz prompt appeared** (a real signal this product gate is likely already satisfied for this account, not a formal API confirmation since none exists). Also reported the account UI defaults to **Cross margin, 20× leverage** on this never-before-configured symbol.

- **Real defect this exposed**: `configureHedgeAccount` sent the leverage/margin-type change requests but nothing verified they actually took effect before an order would have been placed — it trusted the `POST` responses alone. Fixed with `verifyHedgeAccountConfig()`: reads back `GET /fapi/v3/positionRisk` immediately after configuring, refuses to place the hedge order if real leverage/margin type don't match exactly what was requested. Wired into `LiveExecutionAdapter.placeOrder`'s hedge path: resolve position mode → configure → **verify** → place → reconcile. 9 new tests, including the literal 20×/Cross scenario observed this session.
- **Re-confirmed generic sizing** per explicit user clarification ("$40 must not become a hardcoded product amount"): audited `src/mcp/server.ts` and `src/db/index.ts`'s `createStrategy` — `contributionUsd` has no default anywhere in any strategy-creation interface (MCP tool schemas, DB layer); only `hedgeLeverage` has a policy default (2×, from `DEFAULT_HEDGE_LEVERAGE`), never tied to a dollar amount. New test: `createStrategy` persists arbitrary amounts ($17.50, $34, $40, $100, $500, $9999.99) exactly, no rounding/clamping to a "nice" number.
- **115/115 tests passing** (107 prior + 9 verify-config tests − 1 net from consolidation), typecheck clean.
- **Explicitly NOT done**: no order placed, no leverage/margin actually changed on the real account this checkpoint (only the earlier read-only preflight touched the real account, and that was checkpoint 10), nothing synced/deployed to the VPS this checkpoint, live gate unchanged.

## Checkpoint 12 — Automatic Spot->Futures funding: planner, transfer adapter, lifecycle wiring (2026-09-08 ~20:00-20:15 UTC)
User confirmed automatic funding is part of product completeness. Built, but never executed against a real account:

- **Verified real endpoints**: `POST /sapi/v1/asset/transfer` (create), `GET /sapi/v1/asset/transfer` (history) — confirmed against `developers.binance.com`'s Wallet Asset API endpoint list. Exact `type` enum value/full response schema not independently re-fetched field-by-field (docs site limitation this session) — flagged for a final manual check before real use.
- **`planFunding` (`src/binance/fundingReadiness.ts`)**: deterministic — required collateral + configured buffer, actual balance, shared-wallet reservation subtraction, per-cycle and optional period caps, distinct `prefunded`/`auto` behavior. Never uses account NAV, never proposes transferring the full balance.
- **`fundingTransfer.ts`**: real request builders, `assertAutoFundingGate` (separate from `assertLiveTradingGate`), ambiguous-outcome recovery via history lookup (never blind retry), confirmation polling (never trusts the POST response alone).
- **Lifecycle wiring**: `ExecutionAdapter.prepareFunding` (optional method — `PaperExecutionAdapter` doesn't implement it, zero behavior change for paper mode), called by `runContribution` before the hedge leg, throw-captured so a funding failure can't lose the stock leg's record. `LiveExecutionAdapter` implements it for real, defaulting to `{mode:"prefunded", perCycleCapUsd:0}` so all 35 pre-existing tests and every current construction remain unchanged.
- **Schema**: additive migration, `strategies.funding_mode/funding_buffer_usd/funding_per_cycle_cap_usd/funding_period_cap_usd` (default `prefunded`, identical to prior behavior), `executions.funding_step_json`. `createStrategy` refuses `funding_mode='auto'` without an explicit positive cap.
- **MCP**: `create_paper_strategy` accepts the funding-policy fields; real end-to-end run (`scripts/funding-policy-demo.ts`) proved the refusal-without-cap and default-is-prefunded behaviors against the actual server.
- **28 new tests** (`tests/fundingTransfer.test.ts`, `tests/autoFundingLifecycle.test.ts` — the real `LiveExecutionAdapter` class, mocked HTTP only): sufficient balance, exact shortfall, prefunded-never-transfers, per-cycle/period cap enforcement, ambiguous outcome + no-match + query-failure, delayed/never-confirmed transfer, gate on/off, shared-wallet reservation, hedge-deferred-no-funding-needed. **176/176 total, no regressions.**
- **Real blocker found**: the currently-installed live credential has `enableInternalTransfer: false` (from checkpoint 10's real preflight) — even this complete implementation cannot move money until that permission is separately granted.
- **Explicitly NOT done**: no real transfer attempted, no credential changed, "reserved funds across other strategies" is caller-supplied (defaults to 0 — a real DB-aggregation gap, documented not hidden), no dashboard UI (explicitly deprioritized to protect the demo/submission deadline).

## Checkpoint 13 — enableInternalTransfer confirmed live; shared-wallet reservation fixed for real (2026-09-08 ~20:15-20:25 UTC)
- **Real read-only preflight re-run** (`scripts/live-preflight.ts`, as `hedgeos` on the VPS, against the real credential): `enableInternalTransfer: true` — confirmed, not assumed. `enableWithdrawals` still `false`, `ipRestrict` still `true`, unchanged. 10/10 checks passed. Futures wallet still $0 — funding, not permissions, is the remaining blocker.
- **Transfer contract re-verification attempted 3x** against the individual endpoint pages (exact `type` enum, full response schema) — the docs site consistently served only its generic homepage shell to automated fetches for those specific paths (a tool limitation, disclosed honestly, not silently worked around). Endpoint paths themselves remain confirmed via the catalog-listing page (succeeded twice, different sessions).
- **Shared-wallet reservation gap fixed**: `getReservedFuturesUsd(db, excludeStrategyId)` (`src/db/index.ts`) sums every OTHER strategy's real filled/partially-filled hedge collateral from durable `executions` rows (live mode only; a paused strategy's open collateral still counts). `ExecutionAdapter.prepareFunding`'s signature gained an optional `reservedFuturesUsd` override so it's recomputed fresh by `runContribution` on every call (never baked into a long-lived adapter instance, since other strategies' positions change between cycles). 6 new tests (`tests/reservedFuturesUsd.test.ts`): excludes own strategy, sums across multiple others, excludes rejected legs, excludes paper mode, includes paused strategies.
- **182/182 tests passing** (176 + 6), typecheck clean, paper end-to-end (`mcp-smoke-test.ts`) re-verified unaffected.
- **Explicitly NOT done**: no real transfer, no order, no credential/permission change made by this session (the permission change was the account owner's own action, only verified here). Nothing synced to the VPS this checkpoint (execution.ts/liveExecution.ts/db changes remain local-only, consistent with the checkpoint-10 decision not to sync until worker/index.ts and mcp/server.ts's callers are updated together).

## Checkpoint 14 — permitsUniversalTransfer correction; real atomic funding reservation (2026-09-08 ~20:27-20:36 UTC)
- **Corrected an incomplete permission check**: `scripts/live-preflight.ts` only checked `enableInternalTransfer`. A `WebSearch` (docs site itself would not render field-level detail to automated fetches, 6 attempts across two sessions) surfaced a distinct flag, `permitsUniversalTransfer`, that Binance's own community/reference material describes as what `POST /sapi/v1/asset/transfer` actually requires. Added it to the preflight's summary, synced that one file to the VPS, re-ran for real: **`enableInternalTransfer: true` but `permitsUniversalTransfer: false`** — automatic funding remains permission-blocked, corrected from the prior (incomplete) "confirmed" report.
- **Replaced the sum-only reservation with a real atomic claim**: new `funding_reservations` table (additive migration), `reserveFundingAtomically` (`db/index.ts`) — runs inside `db.transaction(...).immediate()`, acquiring a real SQLite RESERVED lock before checking+inserting, so two concurrent connections (the worker and an operator's MCP trigger, or two worker instances) cannot both claim the same dollar. `UNIQUE(strategy_id, cycle_id)` makes a crash-retry of the same cycle idempotent (reuses its reservation, never duplicates). `resolveFundingReservation` frees an uncertain/failed transfer's claim; an abandoned reservation (crash, never resolved) expires from the count after 15 minutes.
- **`ExecutionAdapter.prepareFunding` gained an injected `reserveFn`** (dependency injection, keeping `LiveExecutionAdapter` DB-decoupled by design) — `runContribution.ts` supplies the real `reserveFundingAtomically`/`resolveFundingReservation` wiring.
- **14 new tests** (`tests/reservedFuturesUsd.test.ts` +2 refinements, `tests/reserveFundingAtomically.test.ts` — 8 new, including one with **two genuinely separate SQLite connections to the same on-disk file**, not just two calls in one process, proving the lock is real): simultaneous funding attempts (second correctly loses), both-fit-independently, restart/retry reuses the same reservation, uncertain-transfer release, confirmed-transfer resolution, staleness expiry.
- **190/190 tests passing**, typecheck clean.
- **Explicitly NOT done**: no real transfer, no permission change (the account owner's own actions only, verified here), nothing synced to the VPS beyond the single preflight script.

## Final state
- **190/190 tests passing**, typecheck clean.
- Pushed to `https://github.com/Benita2001/HedgeOS` through commit `68711f8`; checkpoints 12-14's work is committed locally on top of that, not yet pushed.
- VPS: paper-mode worker/dashboard/MCP deployment from checkpoint 10, plus the single updated `live-preflight.ts` script (checkpoint 14) — no live-enabled code deployed anywhere. No credentials created by HedgeOS. No live orders. No agreements signed. No funds transferred. Nothing submitted.

## Explicitly not done (documented, not silently skipped)
- **Live trading**: blocked on human-only prerequisites documented in `LIVE_TRADING_READINESS.md` (bStock/ADGM eligibility, TradFi-Perps agreement, a Futures-enabled credential, and — genuinely unverified — whether the Agentic sub-account itself can hold a Futures position at all). None of these can be resolved without you.
- **Real-time liquidation/mark-to-market monitoring**: the risk module is honest about not having this; building it needs an ongoing price-feed loop against open positions, which doesn't exist yet.
- **Migration tooling for the SQLite schema**: schema changes during this session required resetting the local dev DB file (safe — it only ever held disposable paper-mode demo data). A real migration system would be needed before this could hold anything worth preserving across schema changes; not built, since paper-mode demo data has never needed to survive a schema change in practice.

## Exact next human actions
1. Review this log and the git history (`git log --oneline`, `git diff 7692345..HEAD` for the full diff).
2. If you want live trading pursued: work through `LIVE_TRADING_READINESS.md`'s checklist yourself (Futures onboarding, bStock/ADGM eligibility check, TradFi-Perps agreement, Futures-enabled API key) — none of it can be done on your behalf.
3. If you want the dashboard built next: say so and it's a well-scoped follow-on now that the core + MCP operator interface are solid.
4. Before submitting: confirm your jurisdiction is still clear (already confirmed earlier in this project), and decide whether to record a demo video using the reproducible steps in `README.md`.
5. Nothing has been pushed or submitted — that step is entirely yours to take when ready.
